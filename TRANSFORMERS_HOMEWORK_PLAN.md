# План выполнения домашнего задания: «Трансформеры за 7 дней»

## Цель проекта
За 7 дней пройти полный мини-цикл работы с трансформерами на задаче **sentiment analysis**:
- разобраться с токенизацией;
- получить hidden states и CLS-эмбеддинги;
- визуализировать attention;
- собрать baseline без fine-tuning;
- дообучить модель;
- сравнить baseline и fine-tuned подход;
- провести анализ ошибок и сделать мини-демо.

---

## Что нужно подготовить до старта

### Датасет
Нужен CSV-файл минимум с двумя колонками:

- `text` — текст отзыва / сообщения
- `label` — метка тональности (`0/1` или `0/1/2`)

### Подходящие датасеты
- [x] Выбрать один датасет для проекта
- [x] Проверить, что данные сохранены в формате CSV
- [x] Убедиться, что в файле есть колонки `text` и `label`
- [x] Проверить, сколько классов в задаче: 2 или 3
- [x] Определиться, будет проект на английском или русском языке

Выбранный датасет проекта:
- **IMDb Movie Reviews**
- язык: **английский**
- формат в репозитории: `data/imdb/train.csv` и `data/imdb/test.csv`
- классы: **2** (`0 = negative`, `1 = positive`)

Варианты датасета:
- IMDB Movie Reviews
- Twitter Sentiment Analysis
- SST-2
- Russian Sentiment Dataset

### Базовая структура проекта
- [x] Создать папку проекта и вести всю работу внутри неё
- [x] Хранить код по дням так, чтобы использовать его повторно на следующих этапах
- [x] Сразу договориться об именовании файлов / ноутбуков по дням

Рекомендуемая структура:

```text
.
├── data/
│   └── imdb/
│       ├── train.csv
│       ├── test.csv
│       └── metadata.json
├── notebooks/                  # если работаешь в Jupyter
├── src/                        # если работаешь в .py файлах
├── outputs/
│   ├── attention/
│   ├── reports/
│   └── figures/
├── fine_tuned_model/
├── app.py
├── api.py                      # опционально
├── README.md
└── TRANSFORMERS_HOMEWORK_PLAN.md
```

Текущее состояние репозитория на Day 1 строже минимального шаблона:
- используется `data/imdb/` как основной версионируемый датасет;
- реализован production-style Python-код под `src/`;
- тесты вынесены в `tests/`;
- проверка качества выполняется через `make check`.

---

## День 1 — Архитектура трансформеров и токенизация

### Цель дня
Понять, как текст превращается в токены, и подготовить базовые функции для дальнейшей работы.

### Что нужно сделать
- [x] Установить библиотеки: `transformers`, `torch`
- [x] Создать Python-файл или Jupyter notebook для дня 1
- [x] Импортировать:
  - [x] `AutoTokenizer`
  - [x] `torch`
- [x] Выбрать имя модели:
  - [x] `distilbert-base-uncased` для английского
  - [x] или `distilbert-base-multilingual-cased` для русского
- [x] Загрузить токенизатор через `AutoTokenizer.from_pretrained(model_name)`
- [x] Вывести и посмотреть:
  - [x] `tokenizer.vocab_size`
  - [x] `tokenizer.model_max_length`
- [x] Взять пример текста
- [x] Токенизировать один текст
- [x] Вывести результат токенизации
- [x] Извлечь `input_ids`
- [x] Посчитать количество токенов
- [x] Декодировать токены обратно в текст
- [x] Реализовать функцию `tokenize_texts(texts, max_length=128)` для батчей
- [x] Протестировать батчевую токенизацию на 2–3 текстах
- [x] Вывести shape у `tokens["input_ids"]`
- [x] Вывести `attention_mask`
- [x] Посмотреть специальные токены:
  - [x] `CLS`
  - [x] `SEP`
  - [x] `PAD`
- [x] Показать, как выглядит токенизированный одиночный пример с `return_tensors="pt"`
- [x] Реализовать функцию `explain_tokenization(text, tokenizer)`
- [x] Протестировать `explain_tokenization("Transformers are amazing!", tokenizer)`

### Чекпоинт дня
- [x] Токенизатор загружается без ошибок
- [x] Есть функция `tokenize_texts(...)`
- [x] Есть функция `explain_tokenization(...)`
- [x] Есть понимание, как текст превращается в токены

### Артефакты дня
- [x] Код дня 1 сохранён в проекте
- [x] Подготовлен переиспользуемый токенизатор для следующих дней

Примечание по строгому соответствию формулировке домашки:
- [x] В демо используются точные примеры текста из задания
- [x] Показан отдельный вызов `tokenizer(text, return_tensors="pt")`

---

## День 2 — Получение эмбеддингов (hidden states)

### Цель дня
Научиться получать скрытые состояния из модели и извлекать CLS-эмбеддинги.

### Что нужно сделать
- [ ] Использовать код из дня 1
- [ ] Импортировать `AutoModel`
- [ ] Загрузить модель с тем же `model_name`
- [ ] Перевести модель в режим `eval()`
- [ ] Вывести архитектуру модели через `print(model)`
- [ ] Токенизировать один пример текста
- [ ] Прогнать токены через модель внутри `torch.no_grad()`
- [ ] Проверить:
  - [ ] тип `outputs`
  - [ ] shape `outputs.last_hidden_state`
- [ ] Извлечь CLS-эмбеддинг через `outputs.last_hidden_state[:, 0, :]`
- [ ] Вывести shape CLS-эмбеддинга
- [ ] Вывести первые значения CLS-эмбеддинга
- [ ] Реализовать функцию `get_embeddings(texts, tokenizer, model, batch_size=32)`
- [ ] Внутри функции:
  - [ ] разбивать тексты на батчи
  - [ ] токенизировать батч
  - [ ] получать hidden states
  - [ ] извлекать CLS-токены
  - [ ] сохранять эмбеддинги в список
  - [ ] объединять батчи через `np.vstack(...)`
- [ ] Протестировать функцию на нескольких текстах
- [ ] Проверить итоговый shape эмбеддингов
- [ ] Импортировать `cosine_similarity`
- [ ] Реализовать функцию `similarity(text1, text2, tokenizer, model)`
- [ ] Сравнить похожие по смыслу тексты
- [ ] Сравнить противоположные по смыслу тексты
- [ ] Сравнить значения similarity

### Чекпоинт дня
- [ ] Модель `AutoModel` загружается и работает
- [ ] Есть функция `get_embeddings(...)`
- [ ] Есть функция `similarity(...)`
- [ ] Понятно, что такое hidden states и CLS-токен

### Артефакты дня
- [ ] Код получения эмбеддингов сохранён
- [ ] Есть тестовые примеры сходства текстов

---

## День 3 — Attention-матрицы и визуализация

### Цель дня
Понять механизм внимания и научиться строить визуализации attention.

### Что нужно сделать
- [ ] Использовать код из дней 1 и 2
- [ ] Загрузить модель с `output_attentions=True`
- [ ] Перевести модель в `eval()`
- [ ] Подготовить тестовый текст для анализа attention
- [ ] Токенизировать этот текст
- [ ] Прогнать его через модель внутри `torch.no_grad()`
- [ ] Проверить:
  - [ ] тип `outputs.attentions`
  - [ ] количество слоёв
  - [ ] форму attention для одного слоя
- [ ] Извлечь attention первого слоя
- [ ] Извлечь attention одной головы (`[0, 0]`)
- [ ] Установить `matplotlib` и `seaborn`
- [ ] Импортировать `matplotlib.pyplot`, `seaborn`, `numpy`
- [ ] Реализовать функцию `visualize_attention(tokens, attention, layer=0, head=0)`
- [ ] Внутри функции:
  - [ ] получить attention-матрицу нужного слоя и головы
  - [ ] конвертировать `input_ids` в список токенов
  - [ ] построить `sns.heatmap(...)`
  - [ ] добавить заголовок и подписи осей
  - [ ] сохранить изображение в PNG
- [ ] Визуализировать attention для:
  - [ ] первого слоя
  - [ ] среднего слоя
  - [ ] последнего слоя
- [ ] Сравнить, как меняется внимание между слоями
- [ ] Визуализировать несколько голов одного слоя
- [ ] Проанализировать, как разные головы смотрят на разные связи
- [ ] Проверить attention на тексте с явно выраженной тональностью
- [ ] Посмотреть, на какие слова модель обращает внимание около ключевого слова (`terrible` / аналог)

### Чекпоинт дня
- [ ] Есть функция `visualize_attention(...)`
- [ ] Сохранены несколько attention-графиков
- [ ] Есть понимание, что attention отличается по слоям и головам

### Артефакты дня
- [ ] PNG-файлы с attention-визуализациями
- [ ] Краткие наблюдения по слоям и головам

---

## День 4 — Baseline без обучения трансформера

### Цель дня
Собрать baseline-классификатор на эмбеддингах предобученной модели без fine-tuning.

### Что нужно сделать
- [ ] Импортировать `AutoTokenizer`
- [ ] Загрузить токенизатор (`distilbert-base-uncased` или `bert-base-uncased`)
- [ ] Реализовать / переиспользовать `tokenize_texts(texts, max_length=128)`
- [ ] Протестировать токенизацию на 2–3 примерах
- [ ] Импортировать `AutoModel` и `torch`
- [ ] Загрузить предобученную модель
- [ ] Перевести модель в `eval()`
- [ ] Реализовать функцию `get_cls_embeddings(texts, batch_size=32)`
- [ ] Внутри функции:
  - [ ] разбивать тексты на батчи
  - [ ] токенизировать батч
  - [ ] прогонять через модель в `torch.no_grad()`
  - [ ] извлекать `outputs.last_hidden_state[:, 0, :]`
  - [ ] переводить в numpy
  - [ ] склеивать результат через `np.vstack(...)`
- [ ] Протестировать функцию на небольшом списке текстов
- [ ] Проверить размерность эмбеддингов
- [ ] Загрузить датасет в `DataFrame`
- [ ] Извлечь список текстов и список меток
- [ ] Получить эмбеддинги для всех текстов
- [ ] Импортировать `train_test_split`
- [ ] Разделить данные на train/test в пропорции 80/20
- [ ] Использовать `stratify=y`
- [ ] Использовать `random_state=42`
- [ ] Импортировать `LogisticRegression`
- [ ] Создать модель `LogisticRegression(max_iter=1000, n_jobs=-1)`
- [ ] Обучить baseline-модель
- [ ] Получить предсказания на test
- [ ] Импортировать:
  - [ ] `classification_report`
  - [ ] `f1_score`
- [ ] Вывести classification report
- [ ] Посчитать `macro F1`
- [ ] Сохранить результаты в `baseline_results.txt`

### Дополнительно для последующих дней
- [ ] Сохранить baseline-модель на диск (например, `baseline_model.pkl`)
- [ ] Если используешь дополнительный preprocessing/vectorizer — сохранить и его

### Чекпоинт дня
- [ ] Есть baseline-классификатор
- [ ] Посчитаны метрики baseline
- [ ] Сохранён файл `baseline_results.txt`

### Артефакты дня
- [ ] `baseline_results.txt`
- [ ] Сохранённая baseline-модель

---

## День 5 — Fine-tuning модели

### Цель дня
Дообучить трансформерную модель на выбранном датасете.

### Что нужно сделать
- [ ] Использовать токенизатор и датасет из предыдущих дней
- [ ] Импортировать `Dataset` из `torch.utils.data`
- [ ] Реализовать класс `SentimentDataset`
- [ ] В классе:
  - [ ] сохранить тексты, метки, токенизатор и `max_length`
  - [ ] реализовать `__len__`
  - [ ] реализовать `__getitem__`
  - [ ] токенизировать текст с `padding="max_length"`
  - [ ] использовать `truncation=True`
  - [ ] возвращать `input_ids`, `attention_mask`, `labels`
- [ ] Загрузить CSV через `pandas`
- [ ] Получить `texts` и `labels`
- [ ] Разделить данные на train / validation
- [ ] Использовать `test_size=0.2`, `random_state=42`, `stratify=labels`
- [ ] Создать `train_dataset`
- [ ] Создать `val_dataset`
- [ ] Импортировать `AutoModelForSequenceClassification`
- [ ] Определить `num_labels`
- [ ] Загрузить модель для классификации
- [ ] Создать `DataLoader` для train
- [ ] Создать `DataLoader` для validation
- [ ] Импортировать `AdamW`
- [ ] Импортировать `accuracy_score` и `f1_score`
- [ ] Создать оптимизатор с `lr=2e-5`
- [ ] Определить `device = cuda / cpu`
- [ ] Перенести модель на устройство
- [ ] Реализовать функцию `train_epoch(model, dataloader, optimizer, device)`
- [ ] Внутри функции:
  - [ ] перевести модель в `train()`
  - [ ] обнулять градиенты
  - [ ] переносить батч на устройство
  - [ ] получать `loss`
  - [ ] делать `backward()`
  - [ ] делать `optimizer.step()`
  - [ ] усреднять loss по эпохе
- [ ] Реализовать функцию `evaluate(model, dataloader, device)`
- [ ] Внутри функции:
  - [ ] перевести модель в `eval()`
  - [ ] отключить градиенты
  - [ ] получить логиты
  - [ ] взять `argmax`
  - [ ] собрать предсказания и истинные метки
  - [ ] посчитать accuracy
  - [ ] посчитать macro F1
- [ ] Обучить модель 3–5 эпох
- [ ] На каждой эпохе выводить:
  - [ ] `Train Loss`
  - [ ] `Val Accuracy`
  - [ ] `Val F1`
- [ ] Сохранить модель в `./fine_tuned_model`
- [ ] Сохранить токенизатор в `./fine_tuned_model`
- [ ] Сохранить итоговые метрики в `fine_tuned_results.txt`

### Чекпоинт дня
- [ ] Fine-tuned модель обучена
- [ ] Модель и токенизатор сохранены
- [ ] Метрики записаны в `fine_tuned_results.txt`

### Артефакты дня
- [ ] Папка `fine_tuned_model/`
- [ ] `fine_tuned_results.txt`

---

## День 6 — Инференс и сравнение моделей

### Цель дня
Сравнить baseline-модель и fine-tuned модель на одинаковых примерах и на тестовой выборке.

### Что нужно сделать
- [ ] Загрузить fine-tuned модель из `./fine_tuned_model`
- [ ] Загрузить токенизатор из `./fine_tuned_model`
- [ ] Перевести fine-tuned модель в `eval()`
- [ ] Загрузить baseline-модель (`baseline_model.pkl`)
- [ ] Загрузить baseline-векторизатор / сопутствующий объект, если он использовался
- [ ] Реализовать функцию `predict_fine_tuned(texts, model, tokenizer)`
- [ ] Внутри функции:
  - [ ] поддержать один текст и список текстов
  - [ ] токенизировать текст
  - [ ] прогонять через модель без градиентов
  - [ ] считать `softmax`
  - [ ] получать предсказанный класс
  - [ ] возвращать текст, предсказание и вероятности
- [ ] Реализовать функцию `predict_baseline(texts, model, vectorizer, clean_func=None)`
- [ ] Внутри функции:
  - [ ] поддержать один текст и список текстов
  - [ ] при необходимости очистить текст
  - [ ] получить признаки
  - [ ] получить предсказания
  - [ ] при возможности получить `predict_proba`
  - [ ] вернуть структуру с текстом, предсказанием и вероятностями
- [ ] Подготовить список тестовых примеров
- [ ] Получить предсказания fine-tuned модели
- [ ] Получить предсказания baseline-модели
- [ ] Вывести сравнение по каждому примеру
- [ ] Показать, совпадают ли предсказания двух моделей
- [ ] Загрузить тестовые данные из датасета
- [ ] Получить предсказания fine-tuned модели на test
- [ ] Построить confusion matrix для fine-tuned модели
- [ ] Сохранить график в `confusion_matrix_finetuned.png`
- [ ] Посчитать для fine-tuned:
  - [ ] `classification_report`
  - [ ] `macro F1`
  - [ ] `accuracy`
- [ ] Посчитать те же метрики для baseline
- [ ] Посчитать процент улучшения F1
- [ ] Сохранить результаты сравнения в `comparison_results.txt`

### Чекпоинт дня
- [ ] Есть функции `predict_fine_tuned(...)` и `predict_baseline(...)`
- [ ] Построена confusion matrix
- [ ] Метрики baseline и fine-tuned сравнимы между собой
- [ ] Сохранён файл `comparison_results.txt`

### Артефакты дня
- [ ] `confusion_matrix_finetuned.png`
- [ ] `comparison_results.txt`

---

## День 7 — Анализ ошибок и мини-приложение

### Цель дня
Понять, где модель ошибается, и собрать простое демо для показа результата.

### Что нужно сделать

#### Анализ ошибок
- [ ] Собрать `DataFrame` с колонками:
  - [ ] `text`
  - [ ] `true_label`
  - [ ] `pred_label`
- [ ] Отфильтровать все ошибки предсказания
- [ ] Найти `False Positives`
- [ ] Найти `False Negatives`
- [ ] Вывести количество:
  - [ ] всех ошибок
  - [ ] false positives
  - [ ] false negatives
- [ ] Вывести несколько примеров false positives
- [ ] Вывести несколько примеров false negatives
- [ ] Посчитать длину ошибочных текстов
- [ ] Сравнить среднюю длину ошибочных текстов со средней длиной всех текстов
- [ ] Сохранить разбор в `error_analysis.txt`
- [ ] Добавить собственные наблюдения о паттернах ошибок

#### Демо-приложение на Gradio
- [ ] Установить `gradio`
- [ ] Создать `app.py`
- [ ] Реализовать функцию `predict_sentiment(text)`
- [ ] Внутри функции:
  - [ ] токенизировать входной текст
  - [ ] прогонять через fine-tuned модель
  - [ ] считать вероятности классов
  - [ ] получить предсказанный класс
  - [ ] красиво сформировать текстовый ответ
- [ ] Создать интерфейс `gr.Interface(...)`
- [ ] Указать title и description
- [ ] Запустить приложение локально
- [ ] Проверить, что демо открывается в браузере

#### Альтернатива: FastAPI (опционально)
- [ ] Установить `fastapi` и `uvicorn` (если нужен API-вариант)
- [ ] Создать `api.py`
- [ ] Реализовать `POST /predict`
- [ ] Проверить, что API возвращает предсказание и вероятности

#### Документация
- [ ] Создать `README.md`
- [ ] Описать цель проекта
- [ ] Описать структуру файлов
- [ ] Описать запуск Gradio
- [ ] Описать запуск FastAPI (если реализован)
- [ ] Добавить итоговые метрики
- [ ] Показать пример использования модели в коде
- [ ] Добавить требования к окружению

### Чекпоинт дня
- [ ] Сохранён `error_analysis.txt`
- [ ] Демо-приложение запускается
- [ ] `README.md` оформлен
- [ ] Проект завершён как единый 7-дневный пайплайн

### Артефакты дня
- [ ] `error_analysis.txt`
- [ ] `app.py`
- [ ] `README.md`
- [ ] `api.py` (опционально)

---

## Финальный checklist по всему проекту

### Данные и окружение
- [ ] Выбран и подготовлен датасет
- [x] Окружение настроено
- [x] Все нужные библиотеки установлены

### Исследование трансформеров
- [x] Реализована токенизация
- [ ] Реализовано получение эмбеддингов
- [ ] Реализована визуализация attention

### Модели
- [ ] Собран baseline без fine-tuning
- [ ] Проведён fine-tuning модели
- [ ] Выполнено сравнение baseline и fine-tuned

### Аналитика и демо
- [ ] Проведён анализ ошибок
- [ ] Сделано демо-приложение
- [x] Подготовлена документация

### Финальные артефакты
- [ ] `baseline_results.txt`
- [ ] `fine_tuned_results.txt`
- [ ] `comparison_results.txt`
- [ ] `error_analysis.txt`
- [ ] `fine_tuned_model/`
- [x] `README.md`

---

## Что получится в конце
- рабочий проект по sentiment analysis на трансформерах;
- понимание токенизации, hidden states и attention;
- baseline и fine-tuned решения с метриками;
- анализ ошибок модели;
- мини-приложение для демонстрации результата.
